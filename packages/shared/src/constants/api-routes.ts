export const API_ROUTES = {
  HEALTH: "/health",
  PROFILE: "/profile",
  SCHEMES_SEARCH: "/schemes/search",
  SCHEMES: "/schemes",
} as const;

export const DEFAULT_PORTS = {
  WEB: 3000,
  API: 5000,
} as const;
